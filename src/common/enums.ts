export enum RoomTypeEnum {
  PAIR = 'pair',
  GROUP = 'group',
  ECHO = 'echo',
}

export enum LoggerTypeEnum {
  TRACE = 10,
  DEBUG = 20,
  INFO = 30,
  WARN = 40,
  ERROR = 50,
  FATAL = 60,
}

export enum LoggerColorEnum {
  TRACE = 'white',
  DEBUG = 'blue',
  INFO = 'green',
  WARN = 'orange',
  ERROR = 'red',
  FATAL = 'purple',
}
